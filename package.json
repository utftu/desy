{
  "name": "desy",
  "version": "0.0.4",
  "type": "module",
  "types": "./dist/types/desy.d.ts",
  "main": "./dist/desy.js",
  "files": [
    "dist/**"
  ],
  "scripts": {
    "build": "bun build ./src/desy.ts --outdir ./dist && bun run types",
    "types": "bunx tsc --project tsconfig.types.json",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "bun-types": "^0.6.0",
    "mitata": "^0.1.6",
    "yup": "^1.2.0",
    "zod": "^3.21.4",
    "typescript": "^5.0.0"
  },
  "license": "MIT",
  "dependencies": {
    "prettier": "^3.0.3"
  }
}
