import {Schema} from '../schema/schema.ts';
import {Config} from '../types';

export class DateDesy extends Schema<Date> {
  static date(value, {path}) {
    let date;

    // if () {

    // }
  }

  constructor({context}: Config) {
    super({context});

    this.context.rules.push();
  }
}
